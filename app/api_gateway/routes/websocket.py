"""
WebSocket Route — Real-time push to browser clients.

The spec says: MQTT in (WIS2.0), WebSocket out (browser).
This endpoint pushes live alert updates and forecast refreshes
to connected frontend clients.
"""
