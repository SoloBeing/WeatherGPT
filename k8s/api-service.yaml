apiVersion: v1
kind: Service
metadata:
  name: weathergpt-api
  namespace: weathergpt
  labels:
    app.kubernetes.io/name: weathergpt
    app.kubernetes.io/component: api-gateway
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app.kubernetes.io/name: weathergpt
    app.kubernetes.io/component: api-gateway
